{
  "name": "shenxianzhongzi",
  "private": true,
  "scripts": {
    "build": "cd app && npm run build",
    "deploy": "node scripts/deploy-cloudflare-direct.mjs",
    "pdf:to-images": "node scripts/pdf-to-images.mjs"
  },
  "devDependencies": {
    "pdf-to-img": "^5.0.0",
    "sharp": "^0.33.0"
  }
}
